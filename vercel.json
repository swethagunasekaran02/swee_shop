{
    "builds":[{
        "src":"myproject/wsgi.py",
        "use":"@vercel/python",
        "config":{ "maxLanbdaSize": "15mb", "runtime": "python3.11.3"}
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"myproject/wsgi.py"
        }
    ]
}